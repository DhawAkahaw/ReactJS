module.exports = {
  // Other ESLint configurations...
  
  // Exclude specific files or directories from linting
  ignorePatterns: [
    '**/*.min.js', // Ignore all files with .min.js extension
  ]
};
